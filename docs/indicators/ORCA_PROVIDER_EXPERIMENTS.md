# Exact-event provider experiments

Updated 2026-09-13. Active source: `Orca Trades/Working_Suite/Indicators/OrcaProvider*.cs`.

These opt-in experiments are separate from the existing `OrcaProfileDataProvider` and `OrcaProfileDataCache`. No production indicator discovers their private registry. No hidden-series reduction or startup improvement is established.

## Current components

- Stream/registry/read-budget cores: bounded exact-event storage, stable sequence/generation cursors, exclusive publisher ownership, explicit eviction/coverage, bounded disposable reader batches.
- History admission core (source-only, not wired into consumers): evaluates whether a read snapshot declares a producer-confirmed, fully retained UTC interval covering the consumer's requested history. A completed source-lifecycle pass alone is rejected, whether empty or populated. This is one necessary availability check, not source authentication or completed consumer initialization.
- Identity/session/feed-lifetime cores: immutable effective session and timezone snapshots, isolated continuity epochs, terminal disposal; not authenticated broker/cache provenance.
- History configuration capture (deployed for the opt-in history observer; runtime pending): explicit futures Last Tick-1 BarsRequest configuration, resolved merge policy, bounded rollover snapshot, lookup intent, request inputs and session/clock rules. A history-aware identity overload includes this definition and an owned historical-snapshot ID; legacy probe identities are unchanged. Capture makes no data request and does not certify history.
- Ingestion core: one ordered Last callback lane with supplied trade-time quote classification and tick-direction fallback. Identical callback payloads remain separate events.
- Reader comparison: two independent bounded readers compare exact ordered payloads; this is transport verification, not an independent exchange tape or indicator-calculation comparison.
- `OrcaProviderProbe`: manually added chart indicator, primary `OnMarketData` Last source, no added series or renderer, private 100,000-event retention. Tick Replay supplies historical callbacks when enabled; no Tick Replay means live-only. Session resets use native bar flags. Diagnostics are optional.
- `OrcaProviderSubscriptionProbe`: manually added five-second observation of instrument-level subscription callbacks, bounded detail output and owned-handler cleanup. It publishes nothing and does not establish a snapshot filter.
- `OrcaProviderHistoryProbe`: manually added one-shot repository-only request for 1,000 futures Last Tick-1 bars, explicit unmerged/unadjusted settings, bounded sample reporting, configuration comparison and dispatcher-owned cleanup/30-second timeout. No added series, Update subscription, provider publication, UTC coverage assertion or production cache use. Empty/error/cancelled outcomes stay visible. Count/configuration stability and positive quote values are not atomicity/provenance proof.
- `OrcaProviderConnectionMonitor`: owns one direct `Connection.ConnectionStatusUpdate` handler before the probe acquires its publisher. Retains only the private core owner and bounded value-only notification state, not an indicator, chart, account or connection object.

## Continuity policy

The canonical probe faults terminally on a market-data reset, a notification received through its directly owned platform connection monitor after activation, or a Last callback observed during Transition. The monitor attaches to a fresh empty owner before any publisher is acquired. An atomic exchange immediately after the subscription add accessor returns begins the active continuity interval; notifications that win the atomic setup counter before that boundary are counted, not treated as breaks in a stream that does not yet exist. Startup reports `connection-monitor-active; attachmentNotifications=N; publisherAcquired=False`. This is a local admission boundary, not a claim that setup callbacks are snapshots or that the connection is healthy.

The active direct connection policy intentionally includes all notifications, including unrelated/order-only/unclassified notifications, because affected-feed attribution is unverified. A callback queued during setup but delivered after activation still invalidates. There is no timed grace period, status filtering or automatic recovery. It can stop a valid observation unnecessarily; reload creates a new isolated run. No automatic reconnect, callback buffering, timestamp-based deduplication or guessed handoff is allowed.

Indicator `OnConnectionStatusUpdate` notifications are now diagnostic only. They do not create/recover a feed or override the separately attached monitor. Any active-interval direct notification immediately closes the private core on the notifying thread using bounded core locks; it performs no chart callback, printing or unsubscribe there. The next probe callback/status/removal reports the terminal cause (`phase=Active` versus `phase=AttachmentFailure`) and detaches the exact handler. In a quiet feed the closed core precedes its deferred Output/Diagnostics report. Normal removal also reports `connection-monitor-detached=True`. A failed detach is explicit and retains a helper for retry, with no static chart reference.

Normal historical-to-Realtime transition preserves every accepted callback. The first live event and final removal print bounded reader-comparison summaries, even when Diagnostics is off. Summary PASS confirms delivery of accepted events only; it does not establish historical completeness, missing exchange trades, snapshot provenance or cross-chart compatibility.

## Evidence and next gate

Two revised canonical-probe runs stopped on price=Connecting after historical comparison PASS (latest 2,988,611 events, live=0). Julian confirmed prices continued updating on the chart, including with only the canonical probe installed. This establishes that the probe stopped while the chart continued, not that the notification was harmless or what emitted it. The 2026-09-08 update prints at most eight connection observations, including previous/new price/order status, current connection-object price status, UTC receipt time and lifecycle/counters. Observations may continue after the terminal fault; ingestion never resumes. Current status cannot authenticate older callbacks. The next capture is diagnostic, not an expected clean PASS run.

Julian supplied four historical comparison PASS runs (~2.95 million events each, live=0). A subsequent ES SEP26 subscription observation reported detached=True, published=0, 10 during-add and 200 after-add callbacks, 33 total Last callbacks, zero reset callbacks, elapsed 4.99 seconds. A during-add Last had reset=False. This validates that observation's execution/cleanup, not a universal synchronous snapshot contract.

The latest diagnostic capture showed two `Disconnected->Connecting->Connected` pairs immediately after Realtime while current connection price state was already Connected; historical comparison passed 2,183,537 events and live remained zero. This is consistent with initialization/delayed script notifications but does not prove their origin or authenticate a tick. The direct monitor replaces the script notification as the probe's continuity boundary without relying on current connection status to ignore events.

The next direct-monitor capture showed only successful detachment and a Disconnected->Connecting fault at 2026-09-08T18:09:59.6127379Z. No startup line was visible. This does not prove whether the callback ran during attachment. The follow-up explicitly separates setup from active admission and prints its count, while preserving terminal handling for every post-activation notification.

On 2026-09-09 Julian supplied two successful revised-probe captures. The first compared 2,174,855 historical events plus one live event. A separate full run (`6a1780225f5d43988357277c31c29c5c`) compared 2,175,885 historical events plus 75 live events: verified=2,175,960, readerComparison=PASS at elapsed=25.4s, then connection-monitor-detached=True. Both show attachmentNotifications=2 and connectionGuard=DirectPlatformEventsAfterAttach. Script Disconnected->Connecting->Connected observations did not fault the active direct-monitor run. Revised-code load and this bounded historical/live/normal-removal path are runtime-confirmed; the screenshots do not independently record the F5 keystroke/compiler pane.

The full run retained 100,000 events and evicted 2,075,960 already-consumed events. fullPassRetained=False is expected bounded retention, not failed streaming comparison. passComplete=True is completion of the platform callback pass, while UTC-range-confirmed=false remains an explicit completeness limitation. These results do not establish exchange-tape completeness, historical/live source provenance, all connection environments, or absence of future lifecycle faults. See `docs/handoffs/2026-09-09_orca-provider_live-tail-runtime-evidence.md`.

Julian's subsequent complete output supplies the deliberate-disconnect gate. Instance `e3def9229e6e4fe6bbb3e14643c4f805` received an active direct Connected->Disconnecting notification at 2026-09-09T20:10:28.5554626Z, reported one terminal FAULT and detached its handler. Its script-observation counters remained historical=2,997,715/live=637 after the fault, and no later PASS appears for that ID. Two later distinct IDs independently verified 1,707 and 122 live events and detached normally. All three attached with four setup notifications. This supports terminal active-monitor invalidation/no observed old-run resurrection and fresh live-run operation for the tested sequence; it is not automatic reconnect/recovery by the old owner.

Both later instances reported TickReplay=True but historical=0 and reached phase=Live at elapsed=0.0s. Their PASS verifies live delivery only. The log does not identify why no historical callbacks arrived; do not infer historical restoration from passComplete=True or fullPassRetained=True on an empty historical pass. Detailed evidence and next file-aware scope: `docs/handoffs/2026-09-09_orca-provider_reconnect-runtime-evidence.md`.

No further disconnect test is required for this observed lifecycle gate. Next is historical source/coverage and compatibility admission, including the empty-history case, before cross-chart discovery or first-consumer migration. A standalone market-data subscriber still needs a documented snapshot/incremental boundary. Existing direct/reset/Transition guards remain terminal; no automatic connection changes or hidden-series removal. Full_Suite is not eligible for promotion.

The first local admission slice is now implemented as `OrcaProviderHistoryAdmissionCore.cs`. It does not alter the probe's existing pass-completion fields or runtime behavior. Source-lifecycle/no-history/live-only streams are denied as unverified UTC coverage; explicitly confirmed empty requested intervals remain valid; failed reads, unconfirmed history, out-of-range requirements and lost confirmed history are denied. This metadata check is point-in-time and never skips unread eviction or substitutes for source identity and continued cursor checks. Integration/deployment and historical platform proof remain pending; see `docs/handoffs/2026-09-09_orca-provider_history-admission.md`.

The next source-only slice adds `OrcaProviderHistoryConfigurationCapture.cs` and an additive history-aware identity constructor. Supported capture is explicitly configured futures Last Tick-1 history with split/dividend adjustment disabled. Inherited global/default merge settings, unsupported adjustment schedules and incomplete/duplicate rollover metadata are rejected. Dates remain recorded local trading-day/count-back inputs, not certified UTC intervals. Existing probe key construction, publication, live deployment and consumers remain unchanged. See `docs/handoffs/2026-09-09_orca-provider_history-configuration.md`.

On 2026-09-13 Julian's history observer `b433701f0cd94ec2be57130368dd6c98` reached a successful-error-code request completion but rejected the initial configuration comparison, then reported requestReleased=True at 1.66s. No sample/count evidence was read. Which field changed is unknown; count-back date resolution is only a hypothesis. The narrowly deployed follow-up now prints bounded named before/after differences from the compared snapshots, with before/after-inspection phase labels. Encoding, strict rejection, request settings and cleanup are unchanged. Fresh F5/re-add output is required; see `docs/handoffs/2026-09-13_orca-provider_history-configuration-differences.md`.

On 2026-09-10 the separate `OrcaProviderHistoryProbe` was implemented and narrowly deployed with that capture and its identity dependency. The existing canonical probe and ten non-target provider sources remain unchanged. The observer uses repository-only lookup, prints bounded sample/configuration/cleanup evidence, and publishes nothing. Linked observer checks passed 153 assertions including nested-dispatcher completion/timeout guards; 473 core, 243 canonical-probe and 14 subscription-observer regressions passed; offline platform compilation reported zero errors across 14 sources. F5/load and the first one-chart observation are pending. Do not infer a UTC interval, authentic quotes, atomic snapshot, live handoff or consumer readiness from an OBSERVED result. See `docs/handoffs/2026-09-10_orca-provider_history-observer.md` for deployment evidence and the manual gate.
